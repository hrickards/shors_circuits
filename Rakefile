require 'bundler/setup'
require 'padrino-core/cli/rake'

PadrinoTasks.use(:database)
PadrinoTasks.use(:mongomapper)
PadrinoTasks.init

# See lib/tasks for custom tasks
# Make custom tasks dependent on :environment
